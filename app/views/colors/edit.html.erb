<h1>Editing Color</h1>

<%= render 'form', color: @color %>

<%= link_to BUTTON[:SHOW_NAME], @color, class: 'btn btn-primary' %>
<%= link_to BUTTON[:RETURN_NAME], colors_path, class: 'btn btn-primary' %>
